# Knowledge fixtures

These are synthetic 1.1.7 wire envelopes. IDs use `kb_test_*`, `media_test_*`, and `note_test_*`; URLs and temporary authorization values are fictional and must never be replaced with account data.
